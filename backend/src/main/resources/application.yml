spring:
  application:
    name: backend
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:prod}  # ✅ 환경변수를 우선 적용하고 기본값은 prod
  jackson:
    deserialization:
      fail-on-unknown-properties: false
  servlet:
    multipart:
      max-file-size: 6MB
      max-request-size: 6MB
  cache:
    type: redis
  data:
    redis:
      host: localhost
      port: 6379
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 1093953420579-hvtg3btvbdp6en06g56j9akpk76rjsia.apps.googleusercontent.com
            client-secret: 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
            scope:
              - openid
              - profile
              - email
app:
  frontend:
    base-url: http://localhost:5173
ai:
  python:
    base-url: http://localhost:8000
