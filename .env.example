# Admin / auth
ADMIN_TOKEN=dev-admin-token

# Asset base directories (defaults to project-relative data/* if unset)
CHARACTER_IMAGE_DIR=data/character
IMAGE_BASE_DIR=data/image
AUDIO_BASE_DIR=data/audio

# AI provider
LLM_PROVIDER=gemini

